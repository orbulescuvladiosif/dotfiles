# TypeScript

- Boolean names use `is`, `has`, `can`, or `should` prefixes
- Flatten conditionals with early returns and guard clauses. No nesting beyond one level
- No `_` prefixes — use access modifiers instead
